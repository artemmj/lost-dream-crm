# Хост — имя сервиса в compose-сети (порты БД наружу не публикуются)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB_NAME=db-crm
POSTGRES_HOST=db-crm
POSTGRES_PORT=5432
POSTGRES_ECHO=False

# service-auth — единый источник валидации токена и проверки пермишенов
AUTH_SERVICE_URL=http://service-auth:8000
